% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoresDetect.R
\name{scoresDetectParallelChunks}
\alias{scoresDetectParallelChunks}
\title{Run scoresDetect's per-recording template matching across chunks
in parallel}
\usage{
scoresDetectParallelChunks(
  survey_info,
  recordingRootPath,
  binTemplates,
  corTemplates,
  template_info,
  combos,
  dup_runs,
  dbInsert,
  corMethod,
  showProgress,
  numCores
)
}
\arguments{
\item{survey_info}{A dataframe of recording IDs and filenames, as returned
by getAudioIDs().}

\item{recordingRootPath}{A character string indicating the path to the
directory containing the recordings, can be local or a URL.}

\item{binTemplates}{A binTemplateList object, or NULL.}

\item{corTemplates}{A corTemplateList object, or NULL.}

\item{template_info}{A dataframe of template info, as returned by
getTemplateIDs().}

\item{combos}{A dataframe of existing modeloutputs min scores, used when
dbInsert = TRUE to skip already-run combinations. NULL when dbInsert is
FALSE.}

\item{dup_runs}{A named list of already-run template names per recording,
used when dbInsert = TRUE. NULL when dbInsert is FALSE.}

\item{dbInsert}{Logical flag, mirrors scoresDetect's dbInsert.}

\item{corMethod}{Correlation method for spectrogram cross correlation
templates.}

\item{showProgress}{Logical flag, mirrors scoresDetect's showProgress.}

\item{numCores}{Number of workers to run.}
}
\value{
A dataframe of scores, with the total audio duration analyzed (in
seconds) attached as the "total_duration_sec" attribute.
}
\description{
An internal function. Splits survey_info into numCores
chunks and runs monitoR template matching for each chunk in its own
forked worker (via parallel::mclapply), each in its own temporary working
directory -- monitoR's binMatch()/corMatch() write a shared
"current_audio.wav" side-effect file to the working directory, which
would otherwise collide across concurrent workers.
}
